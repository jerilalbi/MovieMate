import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#db0000',
        },
        background: {
            default: '#000000ff',
        },
    },
});

export default theme;
